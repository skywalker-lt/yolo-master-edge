# Gradle / AGP / NDK build output
build/
.gradle/
.cxx/
*.apk
*.aar

# Per-machine config (never commit)
local.properties
sdk-paths.properties

# Staged model payloads and test images (ship as Release assets, not tracked)
runtime/src/main/assets/models/*/
runtime/src/main/assets/probe.jpg
runtime/src/androidTest/assets/
